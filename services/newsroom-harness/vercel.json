{
	"version": 2,
	"buildCommand": "pnpm --filter @newscraft/shared build && pnpm --filter @newscraft/newsroom-harness build",
	"builds": [
		{
			"src": "api/index.js",
			"use": "@vercel/node"
		}
	],
	"routes": [
		{
			"src": "/(.*)",
			"dest": "api/index.js"
		}
	]
}
